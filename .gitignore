# Python
__pycache__/
*.pyc
.venv/

# Data and generated outputs (too large for git; see README for setup).
# Note: results/ IS committed — it holds viewable sample outputs and the
# per-image metric tables that the submission requires.
data/
outputs/
runs/
*.zip

# Local/internal working files
legacy/
TEAM_CHECKLIST.md
.claude/

# weights/model.pth (2.3 MB, the submitted inference checkpoint) IS committed.
# The training checkpoint carries 4.6 MB of AdamW optimizer state that
# inference never reads, so it stays out of the repository:
weights/best.pth
weights/last.pth

demo_data/

# PowerPoint lock files
~$*

# Recording aid, not a deliverable
DEMO_VIDEO_SCRIPT.md
